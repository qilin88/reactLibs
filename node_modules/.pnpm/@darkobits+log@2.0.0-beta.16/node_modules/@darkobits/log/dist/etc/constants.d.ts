import { LevelDescriptor } from "./types";
/**
 * Symbol attached to prefix objects that allows them to be identified as such.
 */
export declare const IS_PREFIX: unique symbol;
/**
 * Base object used when adding new log levels.
 */
export declare const DEFAULT_LEVEL_OPTIONS: Partial<LevelDescriptor>;
/**
 * How often we will re-write interactive log lines.
 */
export declare const DEFAULT_FRAME_RATE: number;
/**
 * Pattern that will match the last EOL character in a string.
 */
export declare const LAST_EOL_PATTERN: RegExp;
