"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = isDebugNamespace;

var _env = _interopRequireDefault(require("@darkobits/env"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function generateDebugNamespaceRegex(debugEnvVar) {
  const splitNamespaces = String(debugEnvVar).split(/[ ,]/g);

  if (splitNamespaces.includes('*')) {
    return /.*/g;
  }

  const isWildcardNamespace = str => /.*:\*/g.test(str);

  const isDebugNamespacePattern = splitNamespaces.reduce((patterns, curNamespace) => {
    if (isWildcardNamespace(curNamespace)) {
      return [...patterns, `(^${curNamespace}$)|(^${curNamespace}:.*$)`];
    }

    return [...patterns, `(^${curNamespace}$)`];
  }, []).join('|');
  return new RegExp(isDebugNamespacePattern);
}

let isDebugNamespaceRegex;

function isDebugNamespace(testNamespace) {
  if (!_env.default.has('DEBUG')) {
    return false;
  }

  if (!isDebugNamespaceRegex) {
    isDebugNamespaceRegex = generateDebugNamespaceRegex((0, _env.default)('DEBUG'));
  }

  return isDebugNamespaceRegex.test(testNamespace);
}

module.exports = exports.default;
//# sourceMappingURL=is-debug-namespace.js.map